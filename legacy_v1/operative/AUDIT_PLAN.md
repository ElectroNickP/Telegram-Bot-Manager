# План аудита Telegram-Bot-Manager

## 1. Архитектурный анализ
- [x] **Дублирование структуры**: Разобраться с сосуществованием `src/` и корневых папок `core/`, `adapters/`, `apps/`.
    *   *Результат*: Продакшен (`start-prod.py`) использует `src/`. Разработка/Тесты частично используют `core/`. Состояние "Split Brain".
- [x] **Entry Points**: Определить точки входа (`start-prod.py`, `src/app.py`) и какой код они используют.
    *   *Результат*: `start-prod.py` -> `src/app.py`. `pyproject.toml` указывает на `apps.api.app`, который не используется в текущем скрипте запуска.
- [x] **Зависимости**: Проверить чистоту слоев (Clean Architecture).
- [x] **Хранение данных**: Оценить надежность `json_adapter` для продакшена.
    *   *Результат*: JSON файлы используются для конфигов и сессий. Для High Load не подходит.

## 2. Качество кода (Code Quality)
- [x] **Линтеры**: Проверить конфигурацию `ruff` и `black`. Запустить их для оценки количества ошибок.
    *   *Результат*: 2694 ошибки линтера `ruff`. Конфиг устарел.
- [x] **Типизация**: Проверить покрытие типами (`mypy`).
- [x] **Dead Code**: Найти неиспользуемый код (особенно в контексте миграции `src` -> `root`).

## 3. Инфраструктура и Безопасность
- [x] **Docker**: Проанализировать `Dockerfile`, `Dockerfile.fixed`, `Dockerfile.test`.
    *   *Результат*: Dockerfile используются для тестирования деплоя (имитация Ubuntu сервера), а не для контейнеризации приложения (нет entrypoint на запуск сервиса без костылей).
- [x] **Secrets**: Проверить, не хранятся ли секреты в коде или конфигах.
    *   *Результат*: **КРИТИЧНО**. `bot_configs.json` содержит реальные Telegram Token и OpenAI Key.
- [x] **Логирование**: Проверить, куда пишутся логи.
    *   *Результат*: Логи пишутся в корень (`bot.log`, `server.log`).
- [x] **Backup**: Оценить механизм бэкапов конфигов.

## 4. Тестирование
- [x] **Конфигурация**: Анализ `pytest.ini`.
- [x] **Покрытие**: Запустить тесты (если безопасно) или оценить наличие тестов для ключевых модулей.
    *   *Результат*: Тесты есть, но конфиг `conftest.py` жестко привязан к `src`, при этом часть тестов пытается импортировать `core`.

## 5. Документация
- [x] Оценить актуальность `README.md` по сравнению с реальностью кода.
